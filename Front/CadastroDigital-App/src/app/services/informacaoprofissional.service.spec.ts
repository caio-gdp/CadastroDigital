/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { InformacaoProfissionalService } from './informacaoprofissional.service';

describe('Service: Informacaoprofissional', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [InformacaoProfissionalService]
    });
  });

  it('should ...', inject([InformacaoProfissionalService], (service: InformacaoProfissionalService) => {
    expect(service).toBeTruthy();
  }));
});

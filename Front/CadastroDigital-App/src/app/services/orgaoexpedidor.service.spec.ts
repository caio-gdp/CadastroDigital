/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { OrgaoexpedidorService } from './orgaoexpedidor.service';

describe('Service: Orgaoexpedidor', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [OrgaoexpedidorService]
    });
  });

  it('should ...', inject([OrgaoexpedidorService], (service: OrgaoexpedidorService) => {
    expect(service).toBeTruthy();
  }));
});
